import { NeedsFilterPipe } from './needs-filter.pipe';

describe('NeedsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NeedsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
