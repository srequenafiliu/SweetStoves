import { RepiceFilterPipe } from './repice-filter.pipe';

describe('RepiceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RepiceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
